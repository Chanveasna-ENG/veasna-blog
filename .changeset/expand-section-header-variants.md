---
"chanveasna-eng-github-io": minor
---

Expand `SectionHeader` with `big`, `normal`, and `small` variants, optional `subtitle`, and `paragraphVariant` override; complete standardized header migration across sections, cards, and portfolio page.
