---
import {
  type SubtitleTagProps,
  getSubtitleTagClasses
} from './subtitle-tag.utils';

interface Props extends SubtitleTagProps {}

const { as = 'span', ...props } = Astro.props;

const Tag = as;
const classes = getSubtitleTagClasses(props);
---

<Tag class={classes}>
  <slot />
</Tag>
