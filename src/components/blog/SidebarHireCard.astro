---
import MedievalFrame from '../common/MedievalFrame.astro';
import SectionHeader from '../common/SectionHeader.astro';
import PrimaryButton from '../ui/PrimaryButton.astro';

interface Props {
  imageUrl?: string;
}

const { imageUrl = '/images/profile.jpg' } = Astro.props;
---

<div class="w-full">
  <MedievalFrame size="small" background="parchment">
    <div class="flex flex-col items-center text-center">
      <!-- Sharp Headshot Box (clean border, zero shadow) -->
      <div class="w-20 h-20 overflow-hidden border border-ink relative mb-4 bg-parchmentDark/40 flex-shrink-0">
        <img
          src={imageUrl}
          alt="Chanveasna Eng"
          class="w-full h-full object-cover"
          loading="lazy"
        />
      </div>

      <SectionHeader
        size="small"
        align="center"
        title="Start a Project"
        description="Need custom automations, messaging bots, or a web platform? Let's discuss what you're building on a free intro call."
        paragraphSpacing='xs'
        divider={false}
      />

      <div class="mt-3 w-full">
        <PrimaryButton href="/contact" size="md" fullWidth>
          Work With Me &rarr;
        </PrimaryButton>
      </div>
    </div>
  </MedievalFrame>
</div>
