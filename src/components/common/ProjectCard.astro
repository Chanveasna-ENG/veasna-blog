---
import Heading from '../ui/Heading.astro';
import Paragraph from '../ui/Paragraph.astro';
import SecondaryButton from '../ui/SecondaryButton.astro';
import SubtitleTag from '../ui/SubtitleTag.astro';
import MedievalFrame from './MedievalFrame.astro';
import SectionHeader from './SectionHeader.astro';
import {
  type ProjectCardProps,
  resolveProjectCardConfig
} from './project-card.utils';

interface Props extends ProjectCardProps {}

const config = resolveProjectCardConfig(Astro.props);
const { project } = Astro.props;
---

<MedievalFrame size="small" as="article" interactive={true}>
  {
    config.isSlider ? (
      <>
        <div>
          <!-- Project Visual Preview Mockup Box -->
          <div
            class="w-full aspect-[16/10] overflow-hidden border border-ink/40 mb-5 relative bg-parchmentDark/40 pointer-events-none"
          >
            <img
              src={config.imageSrc}
              alt={`${project.title} Architecture Preview`}
              class="w-full h-full object-cover"
              loading="lazy"
            />
            <div class="absolute inset-0 border border-bronze/20 pointer-events-none" />
          </div>

          <SectionHeader
            size="small"
            subtitleVariant="ghost"
            subtitle={config.primaryTag}
            title={project.title}
            description={project.description}
            clampTitle={2}
            clampDescription={3}
          />
        </div>

        <!-- Card Action / Footer Row -->
        <div class={`${config.footerClass} border-t border-ink/15 mt-auto`}>
          <span
            class="font-heading font-semibold text-xs text-bronze uppercase tracking-widest"
          >
            {config.label}
          </span>

          <a
            href={config.href}
            class="portfolio-card-link inline-flex items-center justify-center w-9 h-9 border border-ink text-ink bg-parchment/60 group-hover:bg-bronze group-hover:text-parchment group-hover:border-bronze transition-all"
            aria-label={`Read case study: ${project.title}`}
          >
            <span class="text-sm font-bold">&rarr;</span>
          </a>
        </div>
      </>
    ) : (
      <div class="grid grid-cols-1 md:grid-cols-12 gap-6 sm:gap-8 items-center">
        <!-- Left Column: Preview Image Mockup -->
        <div class="md:col-span-5 w-full">
          <a
            href={config.href}
            class="block group/image focus:outline-none"
            aria-label={`View architecture: ${project.title}`}
          >
            <div
              class="w-full aspect-[16/10] overflow-hidden border border-ink/40 relative bg-parchmentDark/40"
            >
              <img
                src={config.imageSrc}
                alt={`${project.title} Architecture Preview`}
                class="w-full h-full object-cover group-hover/image:scale-105 transition-transform duration-300"
                loading="lazy"
              />
              <div class="absolute inset-0 border border-bronze/20 pointer-events-none" />
            </div>
          </a>
        </div>

        <!-- Right Column: Card Meta, Title, Description & Action -->
        <div class="md:col-span-7 flex flex-col justify-between h-full space-y-4">
          <div>
            <div class="flex flex-wrap items-center gap-2 mb-3">
              {project.tags.map((tag) => (
                <SubtitleTag variant="ghost" size="sm" spacing="none">
                  #{tag}
                </SubtitleTag>
              ))}
            </div>

            <Heading as="h2" size="card" spacing="sm">
              <a
                href={config.href}
                class="hover:text-bronze transition-colors focus:outline-none"
              >
                {project.title}
              </a>
            </Heading>

            <Paragraph variant="small" spacing="none">
              {project.description}
            </Paragraph>
          </div>

          <div class={config.footerClass}>
            <span
              class="font-heading font-semibold text-xs text-bronze uppercase tracking-widest"
            >
              {config.label}
            </span>

            <SecondaryButton
              href={config.href}
              className="text-xs !py-2 !px-4"
            >
              Read Architecture &rarr;
            </SecondaryButton>
          </div>
        </div>
      </div>
    )
  }
</MedievalFrame>
