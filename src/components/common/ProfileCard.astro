---
import Heading from '../ui/Heading.astro';
import Paragraph from '../ui/Paragraph.astro';
import PrimaryButton from '../ui/PrimaryButton.astro';

// Define the expected props for the Profile Card.
// Providing defaults allows it to be used quickly without passing props every time,
// while still allowing overrides if needed.
interface Props {
  name?: string;
  role?: string;
  bio?: string;
  imageUrl?: string;
  email?: string;
}

const {
  name = 'Chanveasna Eng',
  role = 'Systems Architect & Automation Specialist',
  bio = 'I design and build reliable web systems, workflow automations, and custom bots that replace manual data entry and keep operations running smoothly.',
  imageUrl = '/images/profile.jpg', // Assumes you have an image in public/images/
  email = '/contact'
} = Astro.props;
---

<div class="flex flex-col items-center text-center p-6 bg-darkBg border border-gray-700 rounded-lg shadow-sm">
  
  {/* Circular Image Crop */}
  <div class="w-32 h-32 mb-4 overflow-hidden rounded-full border-2 border-gold flex-shrink-0">
    <img 
      src={imageUrl} 
      alt={`Profile picture of ${name}`} 
      class="w-full h-full object-cover"
      loading="lazy"
    />
  </div>

  {/* Name & Role */}
  <Heading as="h3" size="card" color="bronze" spacing="xs">{name}</Heading>
  <Paragraph variant="caption" color="inkMuted" spacing="sm">
    {role}
  </Paragraph>

  {/* Introduction Bio */}
  <Paragraph variant="body" color="inkMuted" spacing="md">
    {bio}
  </Paragraph>

  {/* Action: Primary Button Instance */}
  <div class="mt-auto w-full">
    <PrimaryButton href={email} size="md" fullWidth>
      Get in Touch &rarr;
    </PrimaryButton>
  </div>

</div>