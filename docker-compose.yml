services:
  veasna-blog:
    build: .
    container_name: veasna-blog
    ports:
      - "8080:8080" # Maps port 8080 on your host to 8080 on the Nginx container
    restart: unless-stopped
    # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost:8080"]
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3
